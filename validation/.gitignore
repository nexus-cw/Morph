# AutoMapper source is cloned locally for validation — never committed to Morph.
automapper-v14/
# Harness temp project is rebuilt each run from the cloned source — never committed
# because it contains (rewritten) AutoMapper test code.
harness/
last-run-report.md
last-run-report.md.tmp
*.log
